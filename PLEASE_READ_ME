При разработке логгера подразумевались следующие требования

1)Если входной параметр строка
Выводится префикс «string:» + строка
2)Если последовательно несколько раз вызывается log(string…) 
	A. Повторяющиеся строки выводятся как «string:» + строка + (xN), где N количество 	повторений соответствующей строки
	Б. Не повторяющиеся строки выводятся см. Пункт 1.
3)Если входной параметр типа int, то при последовательном вызове log(int…)
A.Если нет переполнения, выводится общая сумма выходных параметров
Б.Если есть переполнение, выводится максимальное число (столько раз, сколько было переполнение) + остаток, чтобы общая сумма сохранилась верной.
При этом необходимо учитывать как максимальную, так и минимальную границу переполнения.
4)Если входной параметр byte, то выполняются действия аналогичные  пункту 3) но для типа  byte.
При этом как только меняется тип параметра, сохраняется предыдущее сообщение (сейчас выводится на консоль) и статус логгера меняется на ожидание параметров текущего типа

Чтобы логгер перестал ждать сообщения, необходимо явно вызвать метод завершения работы.

